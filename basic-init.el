;; This is a basic emacs init file with limited customizations, to be used when
;; I just want a barebones emacs experience.
;;
;; TO RUN:
;;   emacs -q --load "~/.emacs.d/basic-init.el"

;; Add customizations here....
