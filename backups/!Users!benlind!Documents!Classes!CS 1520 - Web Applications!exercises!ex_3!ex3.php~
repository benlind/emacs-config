<?php

$name = $_POST['name'];

$fp = fopen('names.txt', 'r+');

if (!$fp) {
    die("Could not open file 'names.txt'.");
}

while (($line = fgets($fp)) !== false) {
    echo $line;
    
    fclose($fp);
}
